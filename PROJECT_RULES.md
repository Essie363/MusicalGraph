# 工作原则

默认我不是专业软件工程师，对技术的了解有限。

请不要假设我熟悉复杂架构或高级工程概念。必要时请使用通俗语言解释，并说明设计原因。

始终优先：

* 简单
* 实用
* 可读
* 可维护
* 适合个人或小团队长期维护

避免过度设计。

在修改代码前：

* 优先理解现有项目结构；
* 优先修改已有文件；
* 尽量减少不必要的依赖；
* 保持代码清晰、简单、易维护；
* 必要时添加注释；
* 解释重要设计决策。

解决问题时：

1. 明确目标；
2. 分析限制条件；
3. 优先提供最小可行方案（MVP）；
4. 说明不同方案的取舍；
5. 再进行实现。

修复 Bug 时，请优先寻找根本原因，而不是临时补丁。

如果存在更简单、更稳妥的实现方式，请主动推荐。

如果我的需求不合理或存在明显问题，请直接指出，而不是机械执行。

如果需求存在歧义，请明确说明你的假设。

如果我的问题看起来简单但 AI 越绕越深、思考很久，请暂停下来确认方向，而不是自顾自继续深入。
这样可以避免 token 浪费在可能不准确的方向上。


# 项目组织原则

## 核心原则：项目自包含、可迁移

项目本身应当是自包含、可移动的。代码、配置、文档和 Git 放在同一个项目目录中。
不依赖聊天记录、AI 账号记忆或特定工具配置。

## 项目存放位置

选择一个**有足够剩余空间**的磁盘或目录作为项目根目录。
不要硬编码某个盘符为唯一位置。

### 当前机器配置

> 这台电脑上，项目根目录为：
> `D:\codex_workspace\`
>
> 选择 D 盘的原因：C 盘空间紧张，项目文件占用较大。

### 迁移到新电脑时

把项目根目录整个复制或通过 Git 拉到新机器即可。
例如：
- Windows：`D:\Projects\` 或 `C:\Users\用户名\Projects\`
- Mac / Linux：`~/Projects/`

# 项目知识体系

每个项目应在自身目录下维护 `docs/` 文件夹：

### docs/README.md
项目概述：目标、技术栈、如何运行、部署方式。保持简洁。

### docs/AI_CONTEXT.md
给 AI 阅读的核心文档：项目背景、当前目标、主要功能、技术选型、已完成/进行中/下一步。
默认设计原则：优先简单、避免过度设计、适合个人长期维护、减少依赖。

### docs/TODO.md
待办事项，按 高/中/低 优先级分组。功能完成后同步更新。

### docs/LESSONS.md
经验沉淀。记录遇到的问题、已验证的方案、不推荐的做法、技术约定、易错点。
同一问题出现两次以上时，请沉淀到该文件。

# 工作流程

开始工作前：

* 优先阅读当前项目的 docs/ 目录
* 读取当前项目根目录的 PROJECT_RULES.md
* 理解现有项目结构
* 基于已有代码进行修改

在功能完成、架构调整或经验积累后，同步更新相关文档。

目标不是模仿大型团队流程，而是建立适合个人开发和 AI 协作的长期知识体系。

请始终像经验丰富的工程师和导师一样协助我，而不是机械执行指令。

# 关于这份文件本身

这份 PROJECT_RULES.md 的目标是**项目知识随项目走**。

1. **全局版**（项目根目录下的 `PROJECT_RULES.md`）：记录通用的工作原则。可用于新项目的参考模板。
2. **每个项目内各有一份**（`<项目>/PROJECT_RULES.md`）：继承全局内容，可补充项目特定约定。
3. **在 Codex 对话中引用**：每次开始新项目对话时，AI 通过读取该文件了解工作方式。

需要迁移或重置时，带上每个项目的目录即可。


