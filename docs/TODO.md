# TODO — MusicGraph 待办事项

> 按 高/中/低 优先级分组。功能完成后同步更新并勾选。
> 更新时间：2026-08-10

## 🔴 高优先级

- [x] **修复脚本硬编码路径问题（已完成 2026-08-07）**
  - 现状：`sync.py`、`run_sync.bat`、`check_gaps.py`、`import_*.py`、`fix_*.py` 等均硬编码 `C:\Users\Hp\Documents\Default Project`，与项目实际位置（`E:\AI VibeCoding Project\MusicGraph`）不一致，运行会读写错误位置。
  - 建议：改为基于脚本所在目录的相对路径（如 `os.path.dirname(__file__)`），符合"项目可迁移"原则。
- [x] **确认计划任务 MusicGraphSync 状态（2026-08-07）**
  - 结论：该计划任务**当前不存在**（`schtasks /query` 报错，全量搜索无名称相似任务）。
  - 影响：数据同步目前不会自动执行，需手动 `python sync.py` 或 `run_sync.bat`。
  - 待办：如需恢复每日 6:00 自动同步，需重新创建计划任务（见下方低优先级）。
- [ ] 继续补演员档案（覆盖率约 9%；最活跃缺口：牛博为、周波、温升宝等；百度百科自动抓取被反爬拦截，改用 `data/profile_template.csv` 手动补齐后导入）
- [x] **专业/备注字段语义清理（2026-08-07，fix_major.py）**
  - 42 人的 major 字段混入学校/年级/参演经历；已清理为只保留专业，学校→school、年级→enrollment_year、其余→备注
  - 顺带修复：出生日期字段被误填成学校名的 6 条（黄可/刘乙萱/洪果/孙欣业/孙礼杰/亓振源）

## 🟡 中优先级（V1 功能）

- [x] 演员精彩片段模块（2026-08-11）：新增 moments 表 + import_moments.py（data/moments.json 导入）；演员详情页「精彩片段」轻量列表（标题即外链，点击跳转新窗口；无符号/无按钮/无封面）；图谱聚焦卡同步显示前 3 条；snapshot_moments.csv 快照；回归 54 项全过
- [x] 搜索与聚焦联动修复（2026-08-11）：① 搜索演员改为在图谱中定位（聚焦 + 右侧信息卡滑出），不再直接跳详情页，详情页通过「查看详情」进入；② 详情页「在图谱中查看」返回后信息卡正常弹出（修复 goGraphFocus 同步/异步双重触发导致 side-open 被 applyRoute 清理的问题）；③ 已点亮演员再次单击会重新拉出信息卡（focusActor 幂等分支补 showFocusCard）。回归 58 项全过
- [x] 同学关系自动推导（同校 + 同 enrollment_year，已完成 2026-08-07，70 对入库）
- [x] 前端视觉系统改版（2026-08-10，参考 Vercel 设计规范）：黑底白字、单色优先、暗红点缀（#6B0F1A，黑底可读亮红 #E5484D；图谱演员光点保持金色 #c9a961）；Geist/Geist Mono 本地字体（`web/fonts/`）；去衬线/毛玻璃/药丸/卡片堆叠；图例圆点+文字行；详情页统计条+发丝线；无障碍（跳转链接/焦点环/唯一 h1）；动效默认静止；删除未用 `web/bg.png`（回归 42 + 设计断言 15 全过）
- [x] 图谱页右侧面板简化（2026-08-10）：筛选改为一行紧凑标签，去掉说明文字；聚焦信息卡移到筛选上方，点击演员后第一屏完整可见（回归 42 + 专项 5 全过）
- [x] 关系标签色板调整（2026-08-10）：与暗红主题同一维度的丝绒色板——共演香槟金 / CP 梅子紫 / 好友深海青 / 同公司烟灰蓝 / 伴侣深酒红 / 情侣玫红 / 前任烟灰 / 同学橄榄绿 / 师生灰紫；标签改「小色点 + 中性底」样式（回归 42 全过）
- [x] 演员页个人信息左对齐（2026-08-10）：档案区不再居中窄列，与姓名/统计条同一内容列左对齐（回归 42 全过）
- [x] 备注字段清理（2026-08-10，fix_note_cleanup.py，执行前已备份 music_graph_backup_20260810.db）：删除 80 条冗余职业标签（音乐剧/演员等）；新增 `artists.role` 字段，205 条幕后工种（灯光/舞美/音响/编剧/导演等）迁入；27 条别名/真名迁入 nickname；14 条地名迁入 hometown；导出脚本与前端详情页同步支持 role（回归 42 全过）
- [x] 备注清理第二轮（2026-08-10，fix_note_polish.py）：剥离残留的「演员/音乐剧演员」冗余措辞——删除 2 条纯冗余、剥离 2 条前后缀、4 人职务迁入 role（含 喻越越 拆分单位与职务）；修复一次同名误伤（王婷 ×3 已按 id 恢复）（回归 42 全过）
- [x] 图谱关系线舞台灯式淡入（2026-08-10）：hover/聚焦时关系线不再瞬间出现，改为帧率无关的指数平滑淡入（时间常数 80ms，约 240ms 亮到 95%）+ 6 组轻微错峰（每条最多延迟 90ms），像舞台灯依次亮起；修复聚焦模式下目标随节点亮度缓动导致淡入卡住的问题（回归 42 + 渐变专项全过）
- [x] 图谱页交互整理（2026-08-10）：① 删除关系类型筛选（图例）功能；② 星点按重要度分级（大剧场经历/剧目多样性/角色深耕 → 普通=小星、重要=稍亮、核心=微弱光晕，export_graph.py 导出 imp，2389 人分层 普通1734/重要468/核心187）；③ 搜索框收进顶部 Header（放大镜图标→点击展开，位置在 Home 左侧，Esc/点外收起）；④ 去掉「返回全局」按钮，点击图谱空白处自动返回全局（回归 48 + 布局专项 8 全过）
- [x] 图谱右侧面板改为点击弹出（2026-08-10）：默认无右侧栏、星图撑满全屏；点击演员时面板从右侧 280ms 缓动滑出（ease-out、无弹跳、透明度淡入，遵循设计规范动效调性），画布随右栏出现同步左移 300px，点空白/Esc 收起并恢复全宽；移动端仍为抽屉覆盖（回归 50 + 面板专项 8 全过）
- [x] 图谱首次访问提示 + 「?」帮助入口（2026-08-10）：左下角常驻指南文字改为低调「?」图标（悬停即展开完整探索指南：Hover/Click/Drag/Search，触屏点击切换；外圈提亮、问号保持低调）；首次进入图谱时底部中央出现轻量提示「移动鼠标，探索演员之间的连接」（900ms 延迟淡入上移、停留 3.6s 自动淡出不留占位、附近一颗星点轻呼吸一次），仅首次展示（localStorage 记录）（回归 50 + 专项全过）
- [x] 首页「从哪里开始」场景直达（2026-08-10）：章节卡片点击后跳到图谱并点亮对应内容——01 图谱=全局视图；02 演员=热门 Top 20 点亮（按影响力排序）；03 作品=热门剧目 Top 30（export_graph.py 新增 musicalStats 演出场次/巡演城市，按场次+城市排序，只显示名字、不画线，悬浮/点击按原交互）；04 团体=团体节点点亮（新增团体节点，点击看成员）；图谱页新增场景卡（场景说明 + 返回全局），Esc/点空白/图例返回全局并清理 URL 参数；支持 `#/graph?scene=xxx` 直达与收藏（回归 50 全过）
- [x] 首页品牌文案更新（2026-08-10）：主标题 CAST LIGHT、副标题 Where Every Performer Finds Their Spotlight、眉题 MUSICAL GRAPH · 中国音乐剧演员关系图谱、顶部导航品牌改 MUSICAL GRAPH（回归 50 全过）
- [x] 首页顶图编辑式排版 + 细节修复（2026-08-10）：参考 Kage 排版语言——标题板块贴底、按钮与「向下探索」无缝衔接（自适应屏高）、CAST LIGHT 拆两行大字（遮罩逐行显现）、眉题行加宽字距、数据标签（演员/场次数）移到右下角（手机端隐藏）、细红线分区、侧边竖排加红色细线；底部渐变收窄保证「进入图谱」按钮首屏完整可见（桌面/笔记本/手机验证）；删除 hero 旁「提交补充」链接；顶部导航选中态只加红色下划线（绝对定位），不再改字重/文字高度（回归 50 全过）
- [x] 前端 MVP（2026-08-07）：`web/` 零依赖网页，支持搜索演员/作品 / 关系图 / 档案面板 / 共演展开 / 共同作品 / 参演剧目 / 作品演员表（`export_graph.py` 生成数据）
- [x] 前端架构调整（2026-08-07）：首页图谱只显示人名与关系连线（去掉"共演N场"标签）；点击人物打开独立详情页 `#/actor/ID`（个人资料+关系网络+关系/共演/剧目/团体明细，hash 路由，支持浏览器后退/直达链接）
- [x] 常共演不再出现"自己"（2026-08-07：id 数字/字符串类型不一致导致取错端，统一字符串化 + 双重防御）
- [x] 首页/详情页点击联动（2026-08-07：首页点人物跳详情页、详情页关系网点人物跳转；修复"拖动吞掉点击"，节点与名字都可点，悬停显示手型）
- [x] 首页入场动效优化（2026-08-07：瞬间算好稳定布局 + 0.8 秒平滑展开入场，之后完全静止，不再长时间晃动）
- [x] 首页排版松弛化（2026-08-07：线条变细变淡、节点变小；布局归一化适配屏幕 + 重叠消除，零遮挡，自动缩放保证名字清晰）
- [x] 首页密度减半（2026-08-07：布局面积×2、最小间距 44→62px、平均 237→335px，密度降为 1/2，零遮挡）
- [x] 真实情侣数据分三类入库（2026-08-07：伴侣 16 / 情侣 28 / 前任 25，共 69 对；前端图例独立 伴侣/情侣/前任 三标签；待确认名单 data/couple_skipped.csv 37 对）
- [x] 前端移动端支持（2026-08-07）：触摸操作 + 响应式布局，手机浏览器可用
- [x] 前端首屏优化 + 一键刷新（2026-08-07）：热门演员快捷入口、自动聚焦、refresh_all.py
- [x] 前端关系类型筛选（2026-08-07）：点击图例切换关系类型显示
- [x] 前端角色展示（2026-08-07）：演员参演剧目带角色、作品演员表带角色
- [x] 前端团体视图（2026-08-07）：搜索团体/成员列表/所属团体
- [x] 数据质量审计 + 前端关系合并（2026-08-07）：确认无脏数据；同名演员 15 组为合理多义；同对多名 CP 前端合并显示
- [x] 首页「舞台追光」互动关系图谱（2026-08-07）：节点按影响力加权（出演剧目数/合作人数/关系度，log 归一化 6–18px）；初始景深（中心亮外围暗 + 径向暗角，不启用真实 blur）；hover 追光（焦点放大提亮、一跳邻居点亮、其余淡出 + 跟随信息卡）；单击节点原位聚焦展开（常共演前 15 人 + 参演剧目前 8 部作为剧目节点，分帧力导向收敛）；双击进入详情页；共演线按场次显粗细/浓淡（0.5–2.5px）；Esc/按钮返回全局；export_graph.py 新增 actorMusicalIds/actorCounts 导出；回归 28 项全过
- [x] 网页背景照片 + UI 暗色暖金主题（2026-08-07）：接入舞台背景照片（web/bg.png，cover/fixed）；整体改为暗色暖金配色（深棕半透明顶栏/底栏+毛玻璃、暖白文字、金色演员节点、青绿剧目节点、提亮的关系线颜色），画布透明露出照片，标签加描边保证可读
- [x] 首页「演员光点」视觉系统（2026-08-07）：节点改为径向渐变柔和光点（无边框），重要人物光晕更大更亮（核心 2–5px / 光晕 12–38px）；默认隐藏姓名与全部关系线（黑暗剧院）；hover 光点亮起+姓名浮现+只显示焦点关联线；点击聚焦显示中心姓名（14px）与剧目节点姓名、保留中心网络关系线；景深按距离降亮度/光晕；命中区域 15–22px；60fps；回归 31 项全过
- [x] 首页 Hero Banner（2026-08-07）：顶部固定 Banner 占第一屏上半部（web/banner.png 黑金剧院图）；衬线标题 "Where Stories Meet on Stage" + 副标题；Banner 底部渐变融入纯黑背景，向下滚动自然进入星图；除 Banner 外页面背景纯黑；hover 时焦点与一跳邻居姓名一起显示（回归 31 项全过）
- [x] 首页 UI 位置与图例优化（2026-08-07）：删除顶部搜索条与底部图例条——顶部直接是 Banner，搜索框与热门入口移入 Banner（热门推荐已按用户要求删除）；详情页头部新增搜索框；图例改为星图内浮动面板（右下角），标签改为「彩色圆点 + 文字 + 细边框」样式，悬停边框亮起所属色；放大到接近最大倍率时全部人物姓名显示（zoom≥2.4）
- [x] 删除无数据的关系类型标签（2026-08-07）：师生/同公司/好友 三类关系均为 0 条数据，已从首页图例与详情页图例移除；当前图例仅保留 伴侣/情侣/前任/CP/同学/共演
- [x] 三页面结构调整（2026-08-07）：Home（Banner+项目介绍+Help/Contribute/Contact）/ Graph（关系图谱+搜索）/ Contribute（信息补充页）；顶部导航 Home | Graph | Contribute（hash 路由：#/home #/graph #/contribute，演员详情 #/actor/ID 保留）
- [x] 贡献表单（2026-08-07）：两层分类（补充信息/勘误 × 演员/剧目/关系）动态切换字段，字段对齐数据库（演员：姓名/昵称/生日/学校/年级/专业/籍贯/身高/备注/团体；剧目：名称/日期/城市/剧院/演员与角色；关系：双方+类型+详情）；提交存 localStorage 待审核列表，可导出 JSON / 复制为 GitHub Issue 文本
- [x] GitHub 方案（2026-08-07）：`.github/workflows/sync.yml` 每日 06:00（UTC 22:00）云端增量同步排期并刷新网页数据、自动提交；用户提交经「待审核列表 → 开发者确认 → 录入数据库 → refresh_all.py」闭环；配置 `MG_GITHUB_REPO` 后提交项可一键生成 GitHub Issue 直达链接
- [x] 首页介绍区文案（2026-08-07）：「你可以探索」改为 6 条具体能力（查合作次数/找潜在联系/看履历/看卡司/顺藤摸瓜/找搭档与核心）；移除「可获取的信息」信息卡与首页 Contribute 跳转按钮（入口保留在顶部导航与底部）
- [x] Graph 页工具面板整合 + 配色收敛（2026-08-07）：搜索/关系类型筛选/当前聚焦人物统一到右侧工具面板（画布不再漂浮卡片，仅保留 hover 提示与底部操作提示）；图例增加「全部显示/全部隐藏」；移动端面板为可开合抽屉；配色改为克制的近白暖灰正文（金色仅用于标题/加粗），关系色整体降饱和；回归 37 项全过
- [x] 全站字体与颜色统一（2026-08-07）：标题 Cormorant Garamond SemiBold、正文 Inter Regular（Google Fonts 引入，离线回退系统字体）；正文色 #F5F5F5，金色仅少量点缀（标题/加粗）
- [x] Graph 页交互与布局细节（2026-08-07）：触控板双指滑动=平移、捏合/Ctrl+滚轮=缩放（Windows Chrome/Edge 捏合以 ctrl+wheel 派发；兼容 Mac Safari gesture 事件）；平移方向按网页滚动习惯（上滑=内容向下）；删除图谱页顶部冗余的「关系图谱」标题行，画布直接贴顶（移动端工具按钮改为浮动）
- [x] 首页 Banner 图更换 + 渐变过渡（2026-08-07）：Banner 背景换为新图（web/banner.png，黑金剧院基调），底部 62% 高渐变（0→45%→纯黑）自然融入下方黑色背景；副标题/眉题/各小节标题统一 Cormorant Garamond（含 Contribute 副标题、图例标题、面板/详情页小节标题）
- [x] 人物页顶部信息块边距对齐（2026-08-08）：姓名/别名/身份行与统计条纳入 1080px 居中容器，与其余板块左右边距完全一致
- [x] 视觉系统重构（Theatrical Archive Dark，2026-08-08）：建立全局 design tokens（bg/text/gold/border/关系语义色/字体/圆角）；首页改为 Hero→About→Explore→Contribute 唯一结构（Hero min(680px,82vh)、单 CTA、ABOUT THE ARCHIVE 眉题、4 张可点击探索卡新文案、共建横向条）；人物页新增面包屑/身份行/统计条/「在图谱中查看」（返回图谱并自动聚焦）；关系/团体改轻量面板；按钮 8px/卡片 12px 圆角统一
- [x] 演员页「查合作」功能（2026-08-08）：在「常共演搭档」卡片内提供查询入口——输入任意演员姓名可查与该演员的共演次数、共同剧目（含角色）、首次/最近合作日期；数据源为全部共演对（48,343 对，导出到 web/data_cowork.js，按需加载不拖慢首屏）；无记录时引导去 Contribute
- [x] 首页探索卡片细节（2026-08-08）：删除 4 张卡片内的黄色小圆点；「你可以探索」标题样式经确认后保持原样（小号金色，不与「关于 MusicGraph」统一）；修复回归测试偶发失败（详情页滚动导致关系图不在视口，点击前先滚回）
- [x] 删除首页介绍区底部「进入关系图谱」大按钮（2026-08-08）：入口保留在 Banner 按钮与顶部导航
- [x] 首页布局与探索区改版（2026-08-08）：Banner 高度收窄（40vh）、与介绍区空隙消除（无缝衔接）；「你可以探索」改为 2×2 光点卡片（发光圆点+衬线标题+说明），要点精简为 4 条（查合作次数/挖潜在联系/看履历与卡司/顺藤摸瓜）
- [x] 首页 Hero 标题字体统一 + 删滚动提示（2026-08-08）：英文大标题与副标题统一为 Cormorant Garamond SemiBold（副标题去斜体同字重）；删除「向下滚动 · 了解这个项目」
- [x] 演员个人页再优化（2026-08-08）：删除详情页搜索框与颜色图例；返回按钮改为纯箭头（悬停显示「返回图谱」提示）；信息分区改为 关系/团体半宽 + 参演剧目/常共演全宽双栏列表；关系网络图改为在板块矩形边框内按亲密度随机平铺（明确关系/共演多者靠中心，边界钳制不越界）；修复个人资料卡片未居中导致的左右边距不一致
- [x] CP 标签独立配色（2026-08-07）：CP 与情侣颜色区分——CP 改为柔和紫 #a48bc4（新增 .lg-cp 类），关系图谱亮灯/连线/图例同步使用新色
- [x] 作品/团体弹窗优化（2026-08-07）：改为居中模态弹窗（不再右侧滑出，遮罩点击/Esc 关闭）；全站暗色滚动条；去掉 🎭/👥 符号；演员/角色颜色改为协调的近白色（去掉蓝色）；作品演员表按角色分组——主演在前（该角色轮演演员多为先，启发式）、组内演员按拼音排序、无角色信息者放最后
- [x] 演员个人页排版优化（2026-08-07）：关系网络图改为与首页一致的柔和光点（中心金色强光、明确关系按优先级取关系色、仅共演为中性色）；布局改为「网络图在上 + 信息卡片分栏在下」（两列网格，移动端单列），解决左右长短不匹配；信息顺序调整为 关系→所属团体→参演剧目→常共演搭档；间距与标题层级（Cormorant 小节标题、卡片化分区）优化
- [x] 关系色优先级（2026-08-07）：多关系并存时按 伴侣>情侣>前任>CP>同学>好友>师生>同公司 取点亮颜色（CP 高于同学）
- [x] 聚焦点亮优化（2026-08-07）：点击演员进入聚焦态后，与其相关的演员点亮（0.85 亮度+姓名）并沿用关系色点亮（CP/同学/伴侣等明确关系用对应类型颜色，仅共演仍为金色），无关演员保持暗淡（≈0.05）；hover 追光同步支持关系色
- [ ] 将 SQLite 数据迁移到 Supabase
- [ ] Next.js 前端搭建（可选升级路线；web/ MVP 已实现搜索 + 演员详情页）
- [ ] Cytoscape.js 关系图谱可视化（可选升级路线）

## 🟢 低优先级（V1 增强 / 长期）

- [ ] 用户系统 + 审核工作流（用户提交 → 管理员审核 → 入库）
- [ ] Vercel 部署上线
- [ ] 建立 docs/LESSONS.md 的持续更新习惯（同问题出现两次以上必须记录）
- [x] 快照更新纳入流程（2026-08-07 完成）：数据变更后运行 `python refresh_all.py` 一键刷新快照 + 网页数据
- [ ] 补充真实情侣（couple）数据（用户后续提供）
- [ ] 重新创建计划任务 MusicGraphSync（每日 6:00 运行 `run_sync.bat`；当前任务不存在）