# AI_CONTEXT — MusicGraph 项目上下文（给 AI 阅读）

> 本文档是项目的核心 AI 上下文。开始工作前请先读本项目 `AGENTS.md`、根目录 `PROJECT_RULES.md` 和本文档。
> 设计原则：**优先简单、避免过度设计、适合个人长期维护、减少依赖。**

## 1. 项目背景

做一个**音乐剧演员关系图谱网站**，让粉丝可以探索演员之间的关系，并共同补充、维护音乐剧行业人物资料。

数据来源：
- y.saoju.net API（演出排期，可直连）
- 手动整理（演员档案 / CP / 团体）

## 2. 当前目标（V1）

**做出一个可以搜索演员、查看关系、提交补充，并真实上线运行的网站。**

V1 核心功能：
1. 演员关系星图（搜索演员 → 可视化关系网络 → 点击展开 → 关系类型）
2. 关系详情（关系类型 / 共同作品 / 相关资料）
3. 用户共建（添加演员、补充关系、修正资料 → 提交 → 审核 → 入库）

## 3. 技术选型（全部免费）

| 层 | 技术 |
|---|---|
| 前端 | Next.js + React |
| 关系图 | Cytoscape.js |
| 数据库 | Supabase（现状：SQLite 单文件，待迁移） |
| 部署 | Vercel |

架构：`网页 → Next.js → Cytoscape.js（关系图）→ Supabase（数据库）`
> 现状（2026-08-07）：前端已落地为零依赖 Canvas MVP（`web/`，无需构建、双击/本地服务即开即用）；Next.js / Cytoscape 为后续可选升级路线。

## 4. 已完成 / 进行中 / 下一步

### 已完成
- [x] 演员精彩片段模块（2026-08-11）：新增 moments 表（id/actor_id/title/url/source/created_time，第一阶段 bilibili/xiaohongshu）；import_moments.py 从 data/moments.json 导入；演员详情页新增「✨ 精彩片段」轻量列表（▶标题+来源+查看链接，外链新窗口，无封面/播放窗口）；图谱聚焦卡同步显示前 3 条；snapshot_moments.csv 快照（回归 54 全过）
- [x] 前端视觉系统改版（2026-08-10，参考 Vercel 设计规范）：黑底白字单色优先、暗红点缀（#6B0F1A，黑底可读亮红 #E5484D；图谱演员光点保持金色 #c9a961）；Geist / Geist Mono 字体本地化到 `web/fonts/`（离线可跑，中文走系统字体回退）；去掉衬线标题、毛玻璃、药丸标签、卡片堆叠；图例改「圆点 + 文字」行、详情页统计条与发丝线分隔；新增跳转链接与可见焦点环等无障碍；动效默认静止（尊重 prefers-reduced-motion）；删除未使用的 `web/bg.png`（回归 42 项 + 设计断言 15 项全过）
- [x] 图谱页右侧面板简化（2026-08-10）：筛选改为一行紧凑标签（圆点+文字），去掉说明文字；聚焦信息卡移到筛选区上方，点击演员后第一屏完整可见（回归 42 + 专项 5 全过）
- [x] 关系标签色板调整（2026-08-10）：与暗红主题同一维度的丝绒色板——共演香槟金 / CP 梅子紫 / 好友深海青 / 同公司烟灰蓝 / 伴侣深酒红 / 情侣玫红 / 前任烟灰 / 同学橄榄绿 / 师生灰紫；标签改「小色点 + 中性底」样式（回归 42 全过）
- [x] 演员页个人信息左对齐（2026-08-10）：档案区不再居中窄列，与姓名/统计条同一内容列左对齐（回归 42 全过）
- [x] 备注字段清理（2026-08-10，fix_note_cleanup.py，执行前已备份 music_graph_backup_20260810.db）：删除 80 条冗余职业标签（音乐剧/演员等）；新增 `artists.role` 字段，205 条幕后工种（灯光/舞美/音响/编剧/导演等）迁入；27 条别名/真名迁入 nickname；14 条地名迁入 hometown；导出脚本与前端详情页同步支持 role（回归 42 全过）
- [x] 图谱关系线舞台灯式淡入（2026-08-10）：hover/聚焦时关系线不再瞬间出现，改为帧率无关的指数平滑淡入（时间常数 80ms，约 240ms 亮到 95%）+ 6 组轻微错峰，像舞台灯依次亮起；修复聚焦模式下目标随节点亮度缓动导致淡入卡住的问题（回归 42 + 渐变专项全过）
- [x] 图谱页交互整理（2026-08-10）：① 删除关系类型筛选（图例）功能；② 星点按重要度分级（大剧场经历/剧目多样性/角色深耕 → 普通=小星、重要=稍亮、核心=微弱光晕，export_graph.py 导出 imp，2389 人分层 普通1734/重要468/核心187）；③ 搜索框收进顶部 Header（放大镜图标→点击展开，位置在 Home 左侧，Esc/点外收起）；④ 去掉「返回全局」按钮，点击图谱空白处自动返回全局（回归 48 + 布局专项 8 全过）
- [x] 图谱右侧面板改为点击弹出（2026-08-10）：默认无右侧栏、星图撑满全屏；点击演员时面板从右侧 280ms 缓动滑出（ease-out、无弹跳、透明度淡入，遵循设计规范动效调性），画布随右栏出现同步左移 300px，点空白/Esc 收起并恢复全宽；移动端仍为抽屉覆盖（回归 50 + 面板专项 8 全过）
- [x] 图谱首次访问提示 + 「?」帮助入口（2026-08-10）：左下角常驻指南文字改为低调「?」图标（悬停即展开完整探索指南：Hover/Click/Drag/Search，触屏点击切换；外圈提亮、问号保持低调）；首次进入图谱时底部中央出现轻量提示「移动鼠标，探索演员之间的连接」（900ms 延迟淡入上移、停留 3.6s 自动淡出不留占位、附近一颗星点轻呼吸一次），仅首次展示（localStorage 记录）（回归 50 + 专项全过）
- [x] 首页「从哪里开始」场景直达（2026-08-10）：章节卡片点击后跳到图谱并点亮对应内容——01 图谱=全局视图；02 演员=热门 Top 20 点亮（按影响力排序）；03 作品=热门剧目 Top 30（export_graph.py 新增 musicalStats 演出场次/巡演城市，按场次+城市排序，只显示名字、不画线，悬浮/点击按原交互）；04 团体=团体节点点亮（新增团体节点，点击看成员）；图谱页新增场景卡（场景说明 + 返回全局），Esc/点空白/图例返回全局并清理 URL 参数；支持 `#/graph?scene=xxx` 直达与收藏（回归 50 全过）
- [x] 首页品牌文案更新（2026-08-10）：主标题 CAST LIGHT、副标题 Where Every Performer Finds Their Spotlight、眉题 MUSICAL GRAPH · 中国音乐剧演员关系图谱、顶部导航品牌改 MUSICAL GRAPH（回归 50 全过）
- [x] 首页顶图编辑式排版 + 细节修复（2026-08-10）：参考 Kage 排版语言——标题板块贴底、按钮与「向下探索」无缝衔接（自适应屏高）、CAST LIGHT 拆两行大字（遮罩逐行显现）、眉题行加宽字距、数据标签（演员/场次数）移到右下角（手机端隐藏）、细红线分区、侧边竖排加红色细线；底部渐变收窄保证「进入图谱」按钮首屏完整可见（桌面/笔记本/手机验证）；删除 hero 旁「提交补充」链接；顶部导航选中态只加红色下划线（绝对定位），不再改字重/文字高度（回归 50 全过）
- [x] 演出数据库搭建（34,993 场 / 152,235 条卡司）
- [x] 共演边机器计算（48,343 对）
- [x] 每日自动同步（计划任务 MusicGraphSync，每日 6:00）
- [x] 百度百科自动抓取（部分成功，44 人）
- [x] 手动档案导入（213 人有资料，覆盖率约 9%）
- [x] note 字段拆分为 major + nickname + note
- [x] CP 关系入库（123 条，type=cp；couple 真实情侣暂空）
- [x] 团体表建设（33 个团体 / 69 人次）
- [x] 同学关系自动推导（同校 + 同 enrollment_year，70 对入库）
- [x] 前端 MVP（`web/`：搜索演员/作品 + 关系图 + 档案面板 + 共同作品/参演剧目/作品演员表，零依赖离线可跑）
- [x] 前端移动端支持（触摸操作 + 响应式布局）
- [x] 前端首屏优化（热门演员快捷入口 + 自动聚焦）
- [x] 前端关系类型筛选（点击图例切换）
- [x] 前端角色展示（参演剧目/作品演员表带角色名）
- [x] 前端团体视图（搜索团体/成员列表/所属团体）
- [x] 数据质量审计（无脏数据；前端合并同对多名 CP）
- [x] 前端架构调整（首页图谱只显示人名与关系连线，去掉"共演N场"标签；点击人物打开独立详情页 `#/actor/ID`，含个人资料 + 关系网络 + 关系/共演/剧目/团体明细；hash 路由支持后退与直达链接）
- [x] 常共演列表不再出现"自己"（id 数字/字符串 === 比较失败根因修复 + 双重防御，8 位演员抽样验证通过）
- [x] 专业/备注字段语义清理（major 只存专业；学校/年级/备注各归各位；出生日期误填学校 6 条修复）
- [x] 首页入场动效优化（瞬间算好稳定布局 + 0.8 秒平滑展开入场，之后完全静止；最中心的人自动居中）
- [x] 首页排版松弛化（线条更细更淡、节点更小；归一化适配屏幕 + 重叠消除，零遮挡，自动缩放保证名字清晰）
- [x] 首页密度减半（布局面积×2、最小间距 44→62px、平均 237→335px，密度降为 1/2，零遮挡）
- [x] 真实情侣数据分三类入库（伴侣 16 / 情侣 28 / 前任 25，共 69 对；前端图例独立三个标签，可分别筛选）
- [x] 百科档案合并（merge_baike.py，19 条；uncovered_actors.csv 已更新为 883 人）
- [x] 人物页顶部边距对齐（2026-08-08）：姓名/身份/统计条与其余板块左右对齐
- [x] 视觉系统重构（2026-08-08）：design tokens、首页 Hero→About→Explore→Contribute、人物页概览+在图谱中查看、轻量面板、圆角统一
- [x] 演员页「查合作」（2026-08-08）：常共演卡内查询任意共演对象，显示次数/共同剧目/日期；data_cowork.js 按需加载
- [x] 首页探索卡片细节（2026-08-08）：删卡片内黄点；h3 标题保持原样；回归偶发修复
- [x] 删除首页介绍区底部进入图谱按钮（2026-08-08）
- [x] 首页布局+探索区（2026-08-08）：Banner 与介绍区无缝衔接；探索区 4 张光点卡片
- [x] 首页 Hero 标题字体统一（2026-08-08）：副标题与大标题同款 Cormorant Garamond SemiBold；删除滚动提示行
- [x] 演员个人页再优化（2026-08-08）：去详情页搜索与图例；箭头返回+悬停提示；分区 关系/团体半宽+剧目/合作全宽双栏；网络图矩形内亲密度平铺不越界；边距统一
- [x] CP 独立配色（2026-08-07）：CP=柔和紫 #a48bc4，与情侣玫瑰色区分，图谱亮灯同步
- [x] 作品/团体弹窗（2026-08-07）：居中模态、暗色滚动条、去 emoji、协调文字色、演员表按角色分组（主演在前/组内拼音序）
- [x] 演员个人页排版（2026-08-07）：网络图光点化+关系色；图上信息下两列卡片布局；顺序 关系→团体→剧目→合作
- [x] 关系色优先级（2026-08-07）：伴侣>情侣>前任>CP>同学>好友>师生>同公司（CP 高于同学）
- [x] 聚焦点亮（2026-08-07）：聚焦态相关演员点亮并显示姓名，明确关系用关系色点亮（仅共演保持金色），无关演员暗淡
- [x] Banner 换图 + 渐变 + 字体统一（2026-08-07）：首页 Banner 用新背景图并底部渐变融入黑；副标题/小标题全部 Cormorant Garamond
- [x] Graph 页交互细节（2026-08-07）：双指滑动平移/捏合缩放（含 Safari gesture 兼容），方向按网页滚动习惯；删除顶部冗余标题行
- [x] 全站字体统一（2026-08-07）：标题 Cormorant Garamond SemiBold、正文 Inter Regular；正文 #F5F5F5、金色少量点缀
- [x] Graph 页工具面板 + 配色收敛（2026-08-07）：搜索/筛选/聚焦信息统一右侧面板（移动端抽屉），图例加全部显示/隐藏；正文近白暖灰、金色仅标题加粗、关系色降饱和
- [x] 首页介绍区（2026-08-07）：「你可以探索」6 条具体能力；移除信息卡与首页 Contribute 按钮
- [x] 三页面结构 + GitHub 方案（2026-08-07）：Home/Graph/Contribute 三页 + 顶部导航；贡献表单两层分类动态字段、提交入本地待审核列表、可导出 JSON/复制为 Issue 文本；新增 `.github/workflows/sync.yml` 每日自动同步并提交；配置 MG_GITHUB_REPO 可一键开 GitHub Issue
- [x] 删除无数据的关系类型标签（2026-08-07）：师生/同公司/好友 0 条数据，从首页与详情页图例移除；保留 伴侣/情侣/前任/CP/同学/共演
- [x] 首页 UI 位置与图例优化（2026-08-07）：删除顶部搜索条/底部图例条，顶部直接 Banner（搜索入 Banner、热门推荐删除）；详情页加搜索；图例浮动面板+圆点标签样式；放大到接近最大时显示全部姓名
- [x] 首页 Hero Banner + 纯黑背景（2026-08-07）：顶部 Banner（banner.png、衬线主标题/副标题、底部渐变融入纯黑）；除 Banner 外页面背景纯黑；向下滚动自然进入星图；hover 时相连人物姓名一起显示
- [x] 首页「演员光点」视觉系统（2026-08-07）：节点改为柔和光点（radialGradient 无边框），影响力由光效表达（核心 2–5px/光晕 12–38px）；默认隐藏姓名与关系线；hover 光点亮起+姓名浮现+只显示关联线；聚焦模式显示中心姓名/剧目姓名与中心网络；景深降亮；命中区域 15–22px
- [x] 网页背景照片 + UI 暗色暖金主题（2026-08-07）：背景照片接入（web/bg.png，cover/fixed）；暗色暖金主题（深棕半透明毛玻璃顶栏/底栏、暖白文字、金色演员节点、青绿剧目节点、提亮关系色、标签描边、画布透明露出照片）
- [x] 首页「舞台追光」互动关系图谱（2026-08-07）：节点影响力加权（出演剧目数/合作人数/关系度 → 半径 6–18px）；初始景深（中心亮外围暗+径向暗角）；hover 追光（焦点放大提亮、一跳邻居点亮、其余淡出+跟随信息卡）；单击聚焦展开（常共演 15 人 + 剧目 8 部作为琥珀色剧目节点）；双击进详情页；共演线按场次显粗细；Esc/按钮返回全局；`export_graph.py` 新增 `actorMusicalIds`/`actorCounts`；回归 28 项全过
- [x] 快照导出脚本（snapshot_export.py → data/snapshot_*.csv）

### 进行中
- 无（文档体系建设中）

### 下一步（详见 TODO.md）
- ~~修复脚本硬编码路径问题~~（已完成：所有脚本改为基于自身目录，数据 `*_raw.json` 已入 `data/api_raw/`）
- 继续补演员档案（覆盖率约 9%；`data/uncovered_actors.csv` 已更新，最活跃缺口：牛博为/周波/温升宝）
- ~~同学关系自动推导~~（已完成，70 对入库）
- SQLite → Supabase 迁移
- Next.js 前端搭建（可选升级路线；`web/` 零依赖 MVP 已实现搜索 + 演员详情页）
- Cytoscape.js 图谱可视化（可选升级路线；当前为零依赖 Canvas）
- 用户系统 + 审核工作流（relations.status: pending → approved/rejected）
- Vercel 部署上线

## 5. 数据库核心表（music_graph.db，约 14MB）

### artists（演员档案，4,532 行）
`id | name | nickname | birth_date | major | school | hometown | enrollment_year | height | note | role | is_actor`

### shows / show_casts（演出排期）
`shows: id | date | time | city | musical | theatre`（34,993 场）
`show_casts: show_id | artist_id | role`（152,235 条）

### co_work_edges（共演边，机器推导，48,343 对）
`actor_a | actor_b | co_show_count | co_musical_count | first_co_date | last_co_date`

### relations（用户关系，268 条）
`type_id | actor_a | actor_b | detail | source_type | status`
- type_id → relation_types：cp（粉丝组合）/ classmate / friend / teacher_student / same_company / co_work / couple（情侣）/ married（伴侣）/ ex（前任）
- source_type：user | derived | media | official
- status：pending | approved | rejected（现有 CP 均为 approved）

### moments（精彩片段，8 条示例）
`id | actor_id | title | url | source | created_time`
- source：bilibili | xiaohongshu（第一阶段）
- 维护：编辑 `data/moments.json` → `python import_moments.py`（整体替换；URL 为占位符示例）

### groups + group_members（33 个 / 69 人次）
`groups: id | name | type (enrollment/cohort/other)`

### 预留表（空）
`users`、`review_logs`（为 web 平台预留）

### 辅助表
`sync_log`（同步记录）、`unresolved_cast`（同名演员歧义待人工确认）

## 6. 目录与关键文件

- `music_graph.db` — 主数据库（SQLite）
- `data/` — 快照 CSV、团体/CP 标签、百科抓取 JSON、演出缓存、活跃度排序、精彩片段 moments.json
- `sync.py` — 每日增量同步（基础表全量刷新 + 排期按日期增量拉取 + 共演边重算）
- `run_sync.bat` — 计划任务入口
- `graph_utils.py` — 共演边重算模块
- `check_gaps.py` — 档案缺口查询
- `import_*.py` — 档案/CP 导入脚本（`import_moments.py`：精彩片段导入）
- `merge_baike.py` — 百科 JSON 合并入库
- `snapshot_export.py` — 导出 data/snapshot_*.csv 快照
- `make_profile_template.py` — 生成档案补齐模板
- `fetch_baike.py` — 百度百科抓取（当前被反爬拦截）
- `refresh_all.py` — 一键刷新：快照 + 网页数据
- `fix_note.py` / `fix_cp.py` / `fix_major.py` — 数据修正脚本（fix_major：专业/学校/年级/备注语义清理）
- `web/` — 前端（index.html + app.js + style.css + data.js + verify.js，verify 为一键回归验证）

## 7. 注意事项

- 所有脚本路径基于自身目录（`Path(__file__).resolve().parent`），项目可整体移动；API 原始快照存放在 `data/api_raw/`。
- SQLite 单文件，无需数据库服务
- y.saoju.net API 可直连；`wapbaike.baidu.com` 偶尔触发反爬验证，需冷却
- 所有 CSV 导出使用 UTF-8-BOM，Excel 可直接双击打开
- 后续用户贡献需走 `pending → approved/rejected` 审核流