# Python 缓存
__pycache__/
*.pyc

# 可重新生成的中间产物/缓存（磁盘保留，不入版本库）
data/raw/
data/actor_shows.csv
data/show_cast.csv

# 密钥/环境变量（绝不提交！）
.env
.env.*
!.env.example

# 本地环境文件
.DS_Store
Thumbs.db

# 前端临时截图（可删除）
.shots/

# 浏览器测试残留（不入库）
.playwright-cli/
# 数据库备份（Git 本身就是版本管理，备份不入库）
music_graph_backup_*.db
